rules_version = '2';

// Deploy to Firebase Console → Firestore → Rules → Publish
// Any signed-in user can read/write (app enforces role hierarchy in code).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
