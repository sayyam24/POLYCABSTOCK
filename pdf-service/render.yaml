services:
  - type: web
    name: pdf-service
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 5001
    healthCheckPath: /health
